import {test} from 'node:test';
import assert from 'node:assert/strict';
import {DatabaseSync} from 'node:sqlite';
import {mkdtempSync,rmSync} from 'node:fs';
import {tmpdir} from 'node:os';
import {join} from 'node:path';
import {migrate} from '../src/infrastructure/sqlite/migrate.js';

function fixture(t) {const db=new DatabaseSync(':memory:');t.after(()=>db.close());migrate(db);db.exec(`INSERT INTO companies VALUES('c1','شرکت آزمایشی'),('c2','شرکت دوم'); INSERT INTO projects(id,company_id,name,customer_name) VALUES('p1','c1','پروژه آزمایشی','مشتری آزمایشی'),('p2','c1','پروژه دوم','مشتری دوم'); INSERT INTO pours VALUES('pour1','c1','p1','2026-09-10T08:00:00Z');`);return db;}
function addInternal(db){db.exec(`INSERT INTO sampling_series(id,company_id,kind,title,purpose,sampled_at,sampler_name,entered_by) VALUES('s1','c1','internal','آزمایش','بررسی','2026-09-10T08:00:00Z','نمونه‌بردار','ثبت‌کننده'); INSERT INTO samples VALUES('sample1','s1',7,'2026-09-17T08:00:00Z');`);}
test('cross-company and cross-project relationships are rejected by SQLite',t=>{const db=fixture(t);assert.throws(()=>db.exec("INSERT INTO pours VALUES('bad','c2','p1','2026-09-10T08:00:00Z')"));const insert=db.prepare('INSERT INTO sampling_series(id,company_id,kind,project_id,pour_id,sampled_at,sampler_name,entered_by) VALUES(?,?,?,?,?,?,?,?)');assert.throws(()=>insert.run('s1','c1','customer','p2','pour1','2026-09-10T08:00:00Z','الف','ب'));insert.run('s1','c1','customer','p1','pour1','2026-09-10T08:00:00Z','الف','ب');});
test('internal records and reserve specimen persist without customer and date',t=>{const db=fixture(t);addInternal(db);db.exec("INSERT INTO samples VALUES('reserve','s1',NULL,NULL)");assert.equal(db.prepare("SELECT due_at FROM samples WHERE id='reserve'").get().due_at,null);assert.throws(()=>db.exec("INSERT INTO samples VALUES('bad','s1',NULL,'2026-09-10')"));});
test('approved result corrections preserve old values and require reasons and sequential revisions',t=>{const db=fixture(t);addInternal(db);const insert=db.prepare('INSERT INTO result_revisions VALUES(?,?,?,?,?,?,?,?,?,?)');insert.run('sample1',1,18,'approved','2026-09-17T08:00:00Z','الف','ب','2026-09-17T09:00:00Z','ج',null);assert.throws(()=>insert.run('sample1',2,19,'approved','2026-09-17T08:00:00Z','الف','ب','2026-09-17T10:00:00Z','ج',null));assert.throws(()=>insert.run('sample1',3,19,'approved','2026-09-17T08:00:00Z','الف','ب','2026-09-17T10:00:00Z','ج','اصلاح'));insert.run('sample1',2,19,'approved','2026-09-17T08:00:00Z','الف','ب','2026-09-17T10:00:00Z','ج','اصلاح ورود');assert.equal(db.prepare('SELECT strength_mpa FROM current_results').get().strength_mpa,19);assert.equal(db.prepare('SELECT strength_mpa FROM result_revisions WHERE revision=1').get().strength_mpa,18);assert.throws(()=>db.exec('UPDATE result_revisions SET strength_mpa=20'));assert.throws(()=>db.exec('DELETE FROM result_revisions'));});
test('engineering QC migration keeps mix versions company-scoped and immutable after use',t=>{const db=fixture(t);db.exec(`INSERT INTO mix_designs(id,company_id,code,title) VALUES('mix1','c1','M-35','طرح ۳۵'); INSERT INTO mix_design_versions(id,mix_design_id,company_id,revision,target_strength_mpa,cement_kg_m3,water_kg_m3,created_at,created_by) VALUES('mix1-r1','mix1','c1',1,35,380,170,'2026-09-10T08:00:00.000Z','مهندس');`);db.prepare(`INSERT INTO pour_qc_specifications(pour_id,company_id,project_id,mix_design_version_id,element_name,specified_strength_mpa,target_slump_mm) VALUES(?,?,?,?,?,?,?)`).run('pour1','c1','p1','mix1-r1','ستون',35,120);assert.equal(db.prepare("SELECT specified_strength_mpa FROM pour_qc_specifications WHERE pour_id='pour1'").get().specified_strength_mpa,35);assert.throws(()=>db.exec("UPDATE mix_design_versions SET cement_kg_m3=390 WHERE id='mix1-r1'"));assert.throws(()=>db.exec("DELETE FROM mix_design_versions WHERE id='mix1-r1'"));db.exec("INSERT INTO mix_designs(id,company_id,code,title) VALUES('mix2','c2','M-40','طرح شرکت دوم')");db.exec("INSERT INTO mix_design_versions(id,mix_design_id,company_id,revision,created_at,created_by) VALUES('mix2-r1','mix2','c2',1,'2026-09-10T08:00:00.000Z','مهندس')");assert.throws(()=>db.prepare(`INSERT INTO pour_qc_specifications(pour_id,company_id,project_id,mix_design_version_id) VALUES(?,?,?,?)`).run('pour1','c1','p1','mix2-r1'));});
test('QC parties and external result events remain company scoped with attachment integrity metadata',t=>{const db=fixture(t);addInternal(db);db.exec(`INSERT INTO sampling_series(id,company_id,kind,title,purpose,sampled_at,sampler_name,entered_by) VALUES('s2','c2','internal','آزمایش دوم','بررسی','2026-09-10T08:00:00Z','نمونه‌بردار','ثبت‌کننده'); INSERT INTO samples VALUES('sample2','s2',7,'2026-09-17T08:00:00Z'); INSERT INTO customers(id,company_id,code,name) VALUES('cust1','c1','C-001','مشتری پایدار'); INSERT INTO concrete_sources(id,company_id,code,name,source_type) VALUES('src1','c1','SRC-1','بچینگ اصلی','internal'); INSERT INTO testing_laboratories(id,company_id,code,name,lab_type) VALUES('lab1','c1','LAB-1','آزمایشگاه مرجع','external'),('lab2','c2','LAB-2','آزمایشگاه شرکت دوم','external');`);db.prepare(`INSERT INTO external_result_events(id,company_id,sample_id,testing_laboratory_id,external_event_id,strength_mpa,tested_at,received_at,received_by) VALUES(?,?,?,?,?,?,?,?,?)`).run('ext1','c1','sample1','lab1','EV-42',31.2,'2026-09-17T08:00:00.000Z','2026-09-17T10:00:00.000Z','کاربر');db.prepare(`INSERT INTO external_result_attachments(id,external_result_event_id,company_id,file_name,media_type,relative_path,sha256,size_bytes,added_at,added_by) VALUES(?,?,?,?,?,?,?,?,?,?)`).run('att1','ext1','c1','report.pdf','application/pdf','external/ext1/report.pdf','a'.repeat(64),2048,'2026-09-17T10:05:00.000Z','کاربر');assert.equal(db.prepare("SELECT external_event_id FROM external_result_events WHERE id='ext1'").get().external_event_id,'EV-42');assert.equal(db.prepare("SELECT size_bytes FROM external_result_attachments WHERE id='att1'").get().size_bytes,2048);assert.throws(()=>db.prepare(`INSERT INTO external_result_events(id,company_id,sample_id,testing_laboratory_id,external_event_id,received_at,received_by) VALUES(?,?,?,?,?,?,?)`).run('badlab','c1','sample1','lab2','EV-X','2026-09-17T10:00:00.000Z','کاربر'));assert.throws(()=>db.prepare(`INSERT INTO external_result_events(id,company_id,sample_id,testing_laboratory_id,external_event_id,received_at,received_by) VALUES(?,?,?,?,?,?,?)`).run('badsample','c1','sample2','lab1','EV-Y','2026-09-17T10:00:00.000Z','کاربر'));assert.throws(()=>db.prepare(`INSERT INTO external_result_attachments(id,external_result_event_id,company_id,file_name,media_type,relative_path,sha256,size_bytes,added_at,added_by) VALUES(?,?,?,?,?,?,?,?,?,?)`).run('badatt','ext1','c1','x.pdf','application/pdf','x.pdf','short',1,'2026-09-17T10:05:00.000Z','کاربر'));});
test('pour QC context links stable customer and source with company integrity and immutability',t=>{const db=fixture(t);db.exec(`INSERT INTO customers(id,company_id,code,name) VALUES('cust1','c1','C-1','مشتری یک'),('cust2','c2','C-2','مشتری دو'); INSERT INTO concrete_sources(id,company_id,code,name,source_type) VALUES('src1','c1','S-1','بچینگ یک','internal'),('src2','c2','S-2','بچینگ دو','external');`);db.prepare(`INSERT INTO pour_qc_contexts(pour_id,company_id,project_id,customer_id,concrete_source_id,created_at,created_by) VALUES(?,?,?,?,?,?,?)`).run('pour1','c1','p1','cust1','src1','2026-09-10T08:01:00.000Z','کاربر');const row=db.prepare("SELECT customer_id,concrete_source_id FROM pour_qc_contexts WHERE pour_id='pour1'").get();assert.equal(row.customer_id,'cust1');assert.equal(row.concrete_source_id,'src1');assert.throws(()=>db.exec("UPDATE pour_qc_contexts SET concrete_source_id=NULL WHERE pour_id='pour1'"));assert.throws(()=>db.exec("DELETE FROM pour_qc_contexts WHERE pour_id='pour1'"));db.exec("INSERT INTO pours VALUES('pour2','c1','p1','2026-09-11T08:00:00Z')");assert.throws(()=>db.prepare(`INSERT INTO pour_qc_contexts(pour_id,company_id,project_id,customer_id,created_at,created_by) VALUES(?,?,?,?,?,?)`).run('pour2','c1','p1','cust2','2026-09-11T08:01:00.000Z','کاربر'));assert.throws(()=>db.prepare(`INSERT INTO pour_qc_contexts(pour_id,company_id,project_id,concrete_source_id,created_at,created_by) VALUES(?,?,?,?,?,?)`).run('pour2','c1','p1','src2','2026-09-11T08:01:00.000Z','کاربر'));});
test('specimen physical measurement ledger is immutable and sequential',t=>{const db=fixture(t);addInternal(db);db.prepare(`INSERT INTO specimen_physical_measurement_revisions(sample_id,revision,shape,length_mm,width_mm,height_mm,diameter_mm,mass_kg,volume_m3,density_kg_m3,reason,entered_by,entered_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).run('sample1',1,'cube',150,150,150,null,8.1,0.003375,2400,null,'کاربر','2026-09-17T08:00:00.000Z');assert.equal(db.prepare("SELECT density_kg_m3 FROM current_specimen_physical_measurements WHERE sample_id='sample1'").get().density_kg_m3,2400);assert.throws(()=>db.prepare(`INSERT INTO specimen_physical_measurement_revisions(sample_id,revision,shape,length_mm,width_mm,height_mm,diameter_mm,mass_kg,volume_m3,density_kg_m3,reason,entered_by,entered_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).run('sample1',3,'cube',150,150,150,null,8.2,0.003375,2429.63,'پرش نسخه','کاربر','2026-09-17T09:00:00.000Z'));assert.throws(()=>db.exec('UPDATE specimen_physical_measurement_revisions SET mass_kg=9'));assert.throws(()=>db.exec('DELETE FROM specimen_physical_measurement_revisions'));});
test('fresh concrete measurement ledger is immutable and sequential',t=>{const db=fixture(t);addInternal(db);db.prepare(`INSERT INTO fresh_concrete_measurement_revisions(series_id,revision,slump_mm,concrete_temperature_c,measured_at,reason,entered_by,entered_at) VALUES(?,?,?,?,?,?,?,?)`).run('s1',1,110,27.5,'2026-09-10T08:10:00.000Z',null,'کاربر','2026-09-10T08:11:00.000Z');assert.equal(db.prepare("SELECT slump_mm FROM current_fresh_concrete_measurements WHERE series_id='s1'").get().slump_mm,110);assert.throws(()=>db.prepare(`INSERT INTO fresh_concrete_measurement_revisions(series_id,revision,slump_mm,concrete_temperature_c,measured_at,reason,entered_by,entered_at) VALUES(?,?,?,?,?,?,?,?)`).run('s1',3,120,28,'2026-09-10T08:12:00.000Z','پرش نسخه','کاربر','2026-09-10T08:13:00.000Z'));assert.throws(()=>db.exec('UPDATE fresh_concrete_measurement_revisions SET slump_mm=130'));assert.throws(()=>db.exec('DELETE FROM fresh_concrete_measurement_revisions'));});
test('reopening a real database retains records and repeated migration is harmless',()=>{const dir=mkdtempSync(join(tmpdir(),'tolou-sqlite-'));let db;try{const file=join(dir,'test.db');db=new DatabaseSync(file);migrate(db);db.prepare('INSERT INTO companies VALUES(?,?)').run('c1','شرکت آزمایشی');db.close();db=null;db=new DatabaseSync(file);migrate(db);migrate(db);assert.equal(db.prepare('SELECT name FROM companies').get().name,'شرکت آزمایشی');assert.equal(db.prepare('SELECT count(*) AS n FROM schema_migrations').get().n,13);for(const table of ['mix_design_versions','witness_schedule_revisions','external_result_events','specimen_physical_measurement_revisions','fresh_concrete_measurement_revisions','pour_qc_contexts','sampling_comparison_parties','sampling_mix_snapshots','comparison_result_revisions','project_strength_requirements','company_profiles'])assert.equal(db.prepare("SELECT count(*) AS n FROM sqlite_master WHERE type='table' AND name=?").get(table).n,1);}finally{db?.close();rmSync(dir,{recursive:true,force:true});}});
test('failed migration rolls back tables and migration registration',t=>{const db=new DatabaseSync(':memory:');t.after(()=>db.close());db.exec('CREATE TABLE projects(id TEXT)');assert.throws(()=>migrate(db));assert.equal(db.prepare("SELECT count(*) AS n FROM sqlite_master WHERE name IN ('companies','schema_migrations')").get().n,0);});
test('database newer than application is refused without mutation',t=>{const db=new DatabaseSync(':memory:');t.after(()=>db.close());migrate(db);db.prepare('INSERT INTO schema_migrations(version,checksum) VALUES(?,?)').run(14,'future');assert.throws(()=>migrate(db),/جدیدتر/);assert.equal(db.prepare('SELECT count(*) AS n FROM schema_migrations').get().n,14);});
test('migration checksum tampering is detected',t=>{const db=new DatabaseSync(':memory:');t.after(()=>db.close());migrate(db);db.prepare('UPDATE schema_migrations SET checksum=? WHERE version=1').run('tampered');assert.throws(()=>migrate(db),/تعریف نسخه/);});
test('gapped migration history is rejected',t=>{const db=new DatabaseSync(':memory:');t.after(()=>db.close());db.exec('CREATE TABLE schema_migrations(version INTEGER PRIMARY KEY, checksum TEXT NOT NULL) STRICT');db.prepare('INSERT INTO schema_migrations(version,checksum) VALUES(?,?)').run(2,'future');assert.throws(()=>migrate(db));});
