import { useCallback, useEffect, useMemo, useState } from 'react';
import { ProjectWorkbench } from './ProjectWorkbench';
import { ReviewWorkspace } from './ReviewWorkspace';
import { EngineeringWorkbench } from './EngineeringWorkbench';
import { QcPartiesWorkbench } from './QcPartiesWorkbench';
import { AnalyticsWorkbench } from './AnalyticsWorkbench';
import { ReportCenter } from './ReportCenter';
import { SamplingWorkspace } from './SamplingWorkspace';
import { FreshConcreteWorkspace } from './FreshConcreteWorkspace';
import { ResultEntryWorkspace } from './ResultEntryWorkspace';
import { SpecimenListWorkspace } from './SpecimenListWorkspace';
import { SystemCenter } from './SystemCenter';
import { SupportWorkspace } from './SupportWorkspace';
import { ManagementWorkspace } from './ManagementWorkspace';
import { isoToPersianLocal } from './jalali';
import type { DashboardSummary, PourContextSummary, PourSpecification, ProjectSummary, SampleSummary } from '../shared/ipc';
import { AnalyticsIcon, DashboardIcon, FreshIcon, MixIcon, ProjectsIcon, ReportsIcon, ResultIcon, ReviewIcon, SamplingIcon, SettingsIcon, SpecimenIcon, SupportIcon } from './RecoveryIcons';
import './ui-recovery.css';
import './dashboard-precision.css';
import './final-polish.css';
import './liquid-background.css';
import './workspace-normalization.css';

type Metric={label:string;value:string;hint:string;tone?:'ok'|'warn'|'danger'};
type WorkspaceView='dashboard'|'management'|'projects'|'sampling'|'fresh'|'specimens'|'results'|'review'|'analytics'|'reports'|'mixes'|'support'|'settings';
type NavItem={id:WorkspaceView;label:string;title:string;icon:typeof DashboardIcon};
type NavSection={label:string;items:NavItem[]};
type DueItem=DashboardSummary['dueSchedule'][number];
type ProjectWithStrength=ProjectSummary&{characteristic_strength_mpa?:number|null;seven_day_reference_mpa?:number|null;twenty_eight_day_reference_mpa?:number|null};
type DueGroup={key:string;projectLabel:string;kind:DueItem['kind'];projectId:string|null;pourId:string|null;ageDays:number|null;status:DueItem['status'];nearestDueAt:string;latestSampledAt:string;count:number;};

const NAV_SECTIONS:NavSection[]=[{label:'',items:[{id:'dashboard',label:'داشبورد',title:'داشبورد کنترل کیفیت',icon:DashboardIcon},{id:'management',label:'مدیریت',title:'مرکز مدیریت کنترل کیفیت',icon:AnalyticsIcon}]},{label:'عملیات',items:[{id:'projects',label:'پروژه‌ها',title:'پروژه‌ها و بتن‌ریزی‌ها',icon:ProjectsIcon},{id:'sampling',label:'نمونه‌برداری',title:'ثبت نمونه‌برداری',icon:SamplingIcon},{id:'results',label:'ثبت نتایج',title:'ثبت نتیجه آزمون',icon:ResultIcon},{id:'fresh',label:'بتن تازه',title:'آزمایش بتن تازه',icon:FreshIcon},{id:'specimens',label:'نمونه‌ها',title:'فهرست نمونه‌ها',icon:SpecimenIcon}]},{label:'کنترل کیفیت',items:[{id:'review',label:'بررسی و تأیید',title:'بررسی و تأیید نتایج',icon:ReviewIcon},{id:'analytics',label:'تحلیل و هشدارها',title:'تحلیل و هشدارهای کنترل کیفیت',icon:AnalyticsIcon},{id:'reports',label:'گزارش‌ها',title:'گزارش‌های کنترل کیفیت',icon:ReportsIcon}]},{label:'مهندسی',items:[{id:'mixes',label:'طرح‌های اختلاط',title:'طرح‌های اختلاط و مشخصات مهندسی',icon:MixIcon},{id:'support',label:'پشتیبانی',title:'پشتیبانی و حقوق نرم‌افزار',icon:SupportIcon},{id:'settings',label:'تنظیمات',title:'تنظیمات و اطلاعات پایه',icon:SettingsIcon}]}];
const emptyDashboard:DashboardSummary={activeProjects:0,totalSeries:0,pendingResults:0,draftResults:0,dueSoonCount:0,overdueCount:0,dueSchedule:[]};
function groupDueItems(items:DueItem[],sampleById:Map<string,SampleSummary>):DueGroup[]{const groups=new Map<string,DueGroup>();for(const item of items){const sample=sampleById.get(item.sampleId);const pourId=sample?.pour_id??null;const projectLabel=item.kind==='customer'?(item.projectName??'پروژه بدون نام'):`کنترل داخلی${item.title?` — ${item.title}`:''}`;const ownerKey=item.kind==='customer'?(item.projectId??projectLabel):(item.title??item.seriesId);const ageKey=item.ageDays===null?'witness':String(item.ageDays);const key=`${item.kind}|${ownerKey}|${pourId??'no-pour'}|${ageKey}|${item.status}`;const current=groups.get(key);if(!current){groups.set(key,{key,projectLabel,kind:item.kind,projectId:item.projectId,pourId,ageDays:item.ageDays,status:item.status,nearestDueAt:item.dueAt,latestSampledAt:item.sampledAt,count:1});continue;}current.count+=1;if(item.dueAt<current.nearestDueAt)current.nearestDueAt=item.dueAt;if(item.sampledAt>current.latestSampledAt)current.latestSampledAt=item.sampledAt;}return[...groups.values()].sort((a,b)=>a.nearestDueAt.localeCompare(b.nearestDueAt));}
const formatMpa=(value:number|null|undefined)=>value==null?'—':`${value.toLocaleString('fa-IR',{maximumFractionDigits:2})} MPa`;

export function App(){
 const[clock,setClock]=useState(new Date());const[dashboard,setDashboard]=useState<DashboardSummary>(emptyDashboard);const[dashboardSamples,setDashboardSamples]=useState<SampleSummary[]>([]);const[dashboardProjects,setDashboardProjects]=useState<ProjectWithStrength[]>([]);const[pourSpecs,setPourSpecs]=useState<Map<string,PourSpecification|null>>(new Map());const[pourContexts,setPourContexts]=useState<Map<string,PourContextSummary|null>>(new Map());const[dataVersion,setDataVersion]=useState(0);const[activeView,setActiveView]=useState<WorkspaceView>('dashboard');const[samplingProjectId,setSamplingProjectId]=useState('');const[reportProjectId,setReportProjectId]=useState('');
 const refreshDashboard=useCallback(async()=>{const[d,s,p]=await Promise.all([window.tolou.dashboard(),window.tolou.listSamples(1000),window.tolou.listProjects()]);if(!d.ok)throw new Error(d.message);if(!s.ok)throw new Error(s.message);if(!p.ok)throw new Error(p.message);setDashboard(d.data);setDashboardSamples(s.data);setDashboardProjects(p.data as ProjectWithStrength[]);const byId=new Map(s.data.map(row=>[row.id,row]));const pourIds=[...new Set(d.data.dueSchedule.map(item=>byId.get(item.sampleId)?.pour_id).filter((x):x is string=>Boolean(x)))];const pairs=await Promise.all(pourIds.map(async pourId=>{const[spec,ctx]=await Promise.all([window.tolou.getPourSpecification(pourId),window.tolou.getPourContext(pourId)]);return{pourId,spec:spec.ok?spec.data:null,ctx:ctx.ok?ctx.data:null};}));setPourSpecs(new Map(pairs.map(x=>[x.pourId,x.spec])));setPourContexts(new Map(pairs.map(x=>[x.pourId,x.ctx])));},[]);
 const dataChanged=useCallback(()=>{setDataVersion(value=>value+1);void refreshDashboard();},[refreshDashboard]);
 useEffect(()=>{const timer=window.setInterval(()=>setClock(new Date()),60_000);void refreshDashboard();return()=>window.clearInterval(timer);},[refreshDashboard]);
 const sampleById=useMemo(()=>new Map(dashboardSamples.map(row=>[row.id,row])),[dashboardSamples]);const groupedDue=useMemo(()=>groupDueItems(dashboard.dueSchedule,sampleById),[dashboard.dueSchedule,sampleById]);const urgentGroups=groupedDue.filter(item=>item.status!=='scheduled');const upcomingGroups=groupedDue.filter(item=>item.status==='scheduled').slice(0,8);const warningProjectCount=new Set(groupedDue.filter(item=>item.status==='warning').map(item=>`${item.kind}|${item.projectId??item.projectLabel}`)).size;const overdueProjectCount=new Set(groupedDue.filter(item=>item.status==='overdue').map(item=>`${item.kind}|${item.projectId??item.projectLabel}`)).size;
 const metrics:Metric[]=useMemo(()=>[{label:'پروژه با موعد تا ۴۸ ساعت',value:warningProjectCount.toLocaleString('fa-IR'),hint:`${dashboard.dueSoonCount.toLocaleString('fa-IR')} نمونه نزدیک موعد`,tone:dashboard.dueSoonCount?'warn':'ok'},{label:'پروژه دارای تأخیر',value:overdueProjectCount.toLocaleString('fa-IR'),hint:`${dashboard.overdueCount.toLocaleString('fa-IR')} نمونه عقب‌افتاده`,tone:dashboard.overdueCount?'danger':'ok'},{label:'منتظر نتیجه',value:dashboard.pendingResults.toLocaleString('fa-IR'),hint:'نمونه‌های بدون نتیجه',tone:dashboard.pendingResults?'warn':'ok'},{label:'منتظر تأیید',value:dashboard.draftResults.toLocaleString('fa-IR'),hint:'پیش‌نویس‌های ثبت‌شده',tone:dashboard.draftResults?'warn':'ok'}],[dashboard,warningProjectCount,overdueProjectCount]);
 const persianDate=useMemo(()=>new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',year:'numeric',month:'long',day:'numeric',hour:'2-digit',minute:'2-digit'}).format(clock),[clock]);const flatItems=NAV_SECTIONS.flatMap(section=>section.items);const activeTitle=flatItems.find(item=>item.id===activeView)?.title??'داشبورد کنترل کیفیت';const goSampling=(projectId?:string)=>{setSamplingProjectId(projectId??'');setActiveView('sampling');};const goReports=(projectId?:string)=>{setReportProjectId(projectId??'');setActiveView('reports');};
 const renderDueGroup=(group:DueGroup)=>{const age=group.ageDays===null?'نمونه شاهد':`نمونه‌های ${group.ageDays.toLocaleString('fa-IR')} روزه`;const status=group.status==='overdue'?'موعد گذشته':group.status==='warning'?'موعد در ۴۸ ساعت آینده':'برنامه آینده';const project=dashboardProjects.find(p=>p.id===group.projectId)??null;const spec=group.pourId?pourSpecs.get(group.pourId)??null:null;const ctx=group.pourId?pourContexts.get(group.pourId)??null:null;const reference=group.ageDays===7?project?.seven_day_reference_mpa:group.ageDays===28?project?.twenty_eight_day_reference_mpa:null;const action=group.status==='overdue'?'ثبت نتیجه':'مشاهده برنامه';return <button type="button" className={`due-row due-row--${group.status} due-row--context`} key={group.key} onClick={()=>setActiveView(group.status==='overdue'?'results':'specimens')}><span className="due-main"><strong>{group.projectLabel}</strong><small>{age} · {group.count.toLocaleString('fa-IR')} نمونه · {status}</small><span className="due-context-meta"><i>عضو: {spec?.element_name||'—'}</i><i>منبع: {ctx?.concrete_source_name||'—'}</i><i>طرح: {spec?.mix_code?`${spec.mix_code}${spec.mix_revision?` / R${spec.mix_revision}`:''}`:'—'}</i></span></span><span className="due-reference"><small>مرجع این سن</small><strong>{formatMpa(reference)}</strong><small>{isoToPersianLocal(group.nearestDueAt)}</small></span><span className="due-action">{action}</span></button>;};
 const renderWorkspace=()=>{switch(activeView){case'dashboard':return <><section className="workspace-intro"><p className="eyebrow eyebrow--accent">امروز</p><h2>مواردی که الان نیاز به توجه دارند</h2><p>هر ردیف به پروژه، Pour، سن آزمون و مرجع مقاومت همان سن متصل است تا اقدام بعدی بدون جست‌وجوی اضافی مشخص باشد.</p></section><section className="metrics-grid" aria-label="وضعیت امروز">{metrics.map(metric=><article className="metric-card glass" key={metric.label}><div className="metric-header"><span>{metric.label}</span><i className={`metric-light metric-light--${metric.tone??'neutral'}`}/></div><strong>{metric.value}</strong><small>{metric.hint}</small></article>)}</section><section className="quick-actions" aria-label="شروع سریع"><button className="quick-action" onClick={()=>setActiveView('projects')}><ProjectsIcon/><span><strong>پروژه و بتن‌ریزی</strong><small>شروع یک پرونده عملیاتی</small></span></button><button className="quick-action" onClick={()=>goSampling()}><SamplingIcon/><span><strong>ثبت نمونه‌برداری</strong><small>ساخت برنامه ۷ و ۲۸ روزه</small></span></button><button className="quick-action" onClick={()=>setActiveView('results')}><ResultIcon/><span><strong>ثبت نتیجه امروز</strong><small>نمونه‌های باز و موعدها</small></span></button><button className="quick-action" onClick={()=>setActiveView('review')}><ReviewIcon/><span><strong>بررسی و تأیید</strong><small>رسیدگی به نتایج ثبت‌شده</small></span></button></section><section className="panel glass panel--wide due-panel"><div className="panel-heading"><div><p className="eyebrow">TODAY QUEUE</p><h3>موعدهای فوری و ۴۸ ساعت آینده</h3></div><button className="text-button" onClick={()=>setActiveView('specimens')}>مشاهده برنامه کامل</button></div>{urgentGroups.length===0?<div className="due-empty">در حال حاضر پروژه‌ای با نمونه عقب‌افتاده یا دارای موعد کمتر از ۴۸ ساعت وجود ندارد.</div>:<div className="due-list">{urgentGroups.map(renderDueGroup)}</div>}</section><section className="panel glass panel--wide due-panel"><div className="panel-heading"><div><p className="eyebrow">برنامه پروژه‌ها</p><h3>موعدهای بعدی ۷ و ۲۸ روزه</h3></div></div>{upcomingGroups.length===0?<div className="due-empty">برای پروژه‌ها موعد آینده‌ای ثبت نشده است.</div>:<div className="due-list">{upcomingGroups.map(renderDueGroup)}</div>}</section></>;case'management':return <ManagementWorkspace refreshKey={dataVersion} onChanged={dataChanged}/>;case'projects':return <><section className="workspace-intro"><p className="eyebrow">عملیات</p><h2>پروژه‌ها و بتن‌ریزی‌ها</h2><p>پروژه را ایجاد کنید، بتن‌ریزی را ثبت کنید و پرونده کامل QC را در یک مسیر مدیریت کنید.</p></section><ProjectWorkbench onChanged={dataChanged} refreshKey={dataVersion} onStartSampling={goSampling} onOpenResults={()=>setActiveView('results')} onOpenReports={goReports}/></>;case'sampling':return <SamplingWorkspace onChanged={dataChanged} refreshKey={dataVersion} initialProjectId={samplingProjectId} onOpenSchedule={()=>setActiveView('specimens')}/>;case'fresh':return <FreshConcreteWorkspace onChanged={dataChanged} refreshKey={dataVersion}/>;case'specimens':return <SpecimenListWorkspace refreshKey={dataVersion} onOpenResult={()=>setActiveView('results')}/>;case'results':return <ResultEntryWorkspace onChanged={dataChanged} refreshKey={dataVersion}/>;case'review':return <ReviewWorkspace onChanged={dataChanged} refreshKey={dataVersion}/>;case'analytics':return <AnalyticsWorkbench refreshKey={dataVersion}/>;case'reports':return <ReportCenter refreshKey={dataVersion} initialProjectId={reportProjectId}/>;case'mixes':return <EngineeringWorkbench onChanged={dataChanged} refreshKey={dataVersion}/>;case'support':return <SupportWorkspace/>;case'settings':return <><section className="workspace-intro"><p className="eyebrow">سیستم</p><h2>تنظیمات و اطلاعات پایه</h2><p>مشخصات سازمانی، پشتیبان‌گیری و داده‌های مرجع از عملیات روزمره جدا شده‌اند.</p></section><SystemCenter mode="settings"/><div className="master-data-shell"><QcPartiesWorkbench onChanged={dataChanged} refreshKey={dataVersion}/></div></>;}};
 return <><SystemCenter mode="onboarding"/><main className="app-shell"><aside className="sidebar glass glass--dark" aria-label="ناوبری اصلی"><div className="brand-block"><div className="brand-mark" aria-hidden="true">T</div><div><strong>طلوع</strong><span>کنترل کیفیت بتن</span></div></div><SystemCenter mode="sidebar"/><nav>{NAV_SECTIONS.map(section=><div className="nav-section" key={section.label||'home'}>{section.label&&<div className="nav-section-label">{section.label}</div>}{section.items.map(item=>{const Icon=item.icon;return <button key={item.id} type="button" className={`nav-item ${activeView===item.id?'nav-item--active':''}`} aria-current={activeView===item.id?'page':undefined} onClick={()=>item.id==='sampling'?goSampling():item.id==='reports'?goReports():setActiveView(item.id)}><Icon/>{item.label}</button>;})}</div>)}</nav></aside><section className="workspace"><header className="topbar glass"><div><p className="eyebrow">Tolou Concrete QC</p><h1>{activeTitle}</h1></div><div className="topbar-actions"><SystemCenter mode="topbar"/><div className="date-chip">{persianDate}</div></div></header><div className="content-grid">{renderWorkspace()}</div></section></main></>;
}